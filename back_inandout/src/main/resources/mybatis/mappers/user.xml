<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="user">
	<!-- No로 유저 찾기 -->
	<select id="findByNo" parameterType="Integer" resultType="uservo">
		<![CDATA[
		select no, name, address, birth, hire_date as hireDate, leave_date as leaveDate, m_leave as mLeave, a_leave as aLeave, phone, position, profile, role 
		from user 
		where no =#{no }
		]]>
	</select>
	
	<select id="findAll" resultType="uservo">
		<![CDATA[
		select no, name, address, birth, hire_date as hireDate, leave_date as leaveDate, m_leave as mLeave, a_leave as aLeave 
		from user 
		]]>
	</select>
</mapper>